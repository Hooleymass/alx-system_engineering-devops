#!/usr/bin/env bash

# Loop through each file in the current directory using a glob pattern
for file in *; do
    if [[ -f "$file" ]]; then
        filename=$(echo "$file" | cut -d '-' -f 2)
        echo "$filename"
    fi
done

